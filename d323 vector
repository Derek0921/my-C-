#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int n;

    while(cin>>n)
       {
           vector<int>v(n);
           for(int &x:v)
            cin>>x;
           sort(v.begin(),v.end());
           for(int x:v)

            cout<<x<<" ";
            cout<<endl;
       }
    return 0;
}
