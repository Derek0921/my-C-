#include <iostream>
#include <sstream>

using namespace std;

int main()
{
    string s;
    while(getline(cin,s))
   {stringstream ss ;
    ss <<s;


    int sum=0,num;
    while(ss>>num)
        sum+=num;
        cout<<sum<<endl;

   }

    return 0;
}
